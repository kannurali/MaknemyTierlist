# Nexus Tier List — редактор

Интерактивный редактор тир-листа по Blox Fruits в стиле макета **Nexus Tier List (сакура)**.
Чистый HTML/CSS/JS, без сборки.

## Как запустить

**Просто дважды кликните `start.bat`** — он поднимет локальный сервер и откроет редактор в браузере
(нужен установленный Python; адрес: http://127.0.0.1:8780/).

> Можно открыть и `index.html` напрямую двойным кликом — редактор работает,
> но кнопка **«Скачать PNG»** при открытии файлом (`file://`) не сработает:
> браузер блокирует экспорт картинок. Поэтому для скачивания используйте `start.bat`.

Альтернатива без `.bat` (в папке проекта):
```
python -m http.server 8780
```
и открыть http://127.0.0.1:8780/

## Возможности

- **Дорожки как в макете**: плашка тира с полутоновыми точками и логотипом по центру,
  дорожка предметов с текстурой, тёмная полоса под ценами, цветные бейджи типов.
- **Тиры**: добавить (`＋ Тир`), переименовать (клик по названию), свой логотип тира (🖼),
  переместить ▲▼, удалить ✕.
- **Предметы**: добавить (`＋` в тире или `＋ Предмет`), перетаскивать между тирами (drag-and-drop).
- **Автосортировка по цене**: при изменении цены предмет сам встаёт на своё место
  (по убыванию, может перейти в другой тир). Выключается тумблером «Автосорт».
  Кнопка «⇅ Сортировать» сортирует все тиры по убыванию цены.
- **Фильтры**: Фрукты / Пермы / Пассы / Скины — можно включать в любых сочетаниях,
  «Все» возвращает всё. Скрытые предметы не попадают в PNG.
- **Язык интерфейса RU / EN** — тумблер в панели. Переводится только интерфейс:
  названия тиров и предметов, реклама, титры и ссылки идут из БД как есть.
  Легенда лежит внутри тирлиста, поэтому **скачанный PNG выходит на выбранном
  языке**. Выбор запоминается; у нового гостя язык берётся из настроек браузера.
  Строки — в `js/i18n.js`, узлы помечены `data-i18n*` в разметке.
- **Редактирование предмета** (клик по ячейке): название, значение, тип (F/S/M/P/GP/CR),
  спрос (🟢🟡🟠🔴), тренд (↑ ⇄ ↓), своя иконка.
- **Логотип в шапке** — фиксированная картинка (флейм БФ + GLH + MK из макета),
  зашит в сайт и не редактируется — знак авторства на каждом экспорте.
- **Реклама** — блок в середине тирлиста, попадает на каждый скрин/скачанное фото.
  Текст редактируется кликом, кнопкой «🖼 Баннер» можно поставить картинку.
- **Команда** — список ролей внизу (Автор, Дизайнер, Аналитик, Помощник аналитика, Кодер сайта).
  Роли и имена редактируются, участников можно добавлять (＋) и убирать (✕).
- **Дата** и тексты редактируются прямо на макете (в режиме редактирования).
- **Режим «Редактирование»** — выключите тумблер, чтобы увидеть чистый вид.
- **Скачать PNG** — сохраняет тир-лист картинкой.
- **Экспорт / Импорт** — сохранить/загрузить весь тир-лист как `.json`.
- **Автосохранение** в браузере (localStorage). **Сброс** — вернуть стандартный шаблон.

## Новости

Отдельная лента на `/news` (адрес чистый — редиректит на `news.html` через
`.htaccess`, ссылки на неё есть в тулбаре обеих страниц и в `sitemap.xml`).

> **Перед деплоем этой ветки таблицу `news` нужно завести на боевой БД вручную**
> (phpMyAdmin или `mysql < schema.sql`) — **до** того, как код уйдёт на сервер.
> Автоматически это не делается: `.cpanel.yml` копирует в веб-рут только
> `public_html/`, `schema.sql` лежит уровнем выше и деплоем не задевается.
> Без таблицы `api/news.php` падает необработанным `PDOException`, то есть
> голым HTTP 500 без единого читаемого слова — а `/news` при этом уже стоит в
> `sitemap.xml` с `changefreq daily`, так что поисковик придёт за ней быстро.

- **Три категории**: Тирлист (изменения трейд-ценностей, ребалансы),
  Игра (апдейты Blox Fruits) и Проект (анонсы, розыгрыши). Список общий для
  фронта и API (`js/news.js`, проверяется на сервере `api/news_save.php`),
  поэтому они не могут разойтись в том, что считают допустимой категорией.
- **Язык — RU / EN**, тот же тумблер, что на тирлисте, и тот же ключ в
  localStorage, так что выбор переносится между страницами. Английский текст
  необязателен: пост можно опубликовать только на русском, а `pickLang`
  (`js/news.js`) откатывается на русский заголовок/текст там, где английского
  ещё нет — лента не показывает пустые карточки.
- **Последние 50 постов, без пагинации**. Это лента объявлений, а не архив:
  константа `NEWS_FEED_LIMIT` в `api/news.php` — единственное место, где
  меняется это число.
- **Своя таблица `news`** (`schema.sql`), отдельно от `tierlist`. Публикация
  поста не трогает JSON и `rev` тирлиста, поэтому не сбрасывает кеш и не
  вызывает у посетителей тирлиста лишнюю перезагрузку данных.

## Структура

```
index.html           разметка (тулбар, шапка, тиры, легенда, футер, команда, модалка)
news.html            разметка ленты новостей (тулбар, сцена, карточки, редактор админа)
css/base.css         общий хром обеих страниц: тулбар, кнопки, чипы, переключатель языка, сцена, лепестки, модалка
css/styles.css       только тирлист: тема, верстка, дорожки, ячейки
css/news.css         только лента: карточки, фильтры, редактор поста (префиксы nw-/ne-)
js/app.js            вся логика редактора
js/news.js           категории, выбор языка и дата поста — общее для ленты и её редактора (без DOM — тестируется в node)
js/news-page.js      загрузка ленты, рендер карточек, фильтры и редактор админа на news.html
js/i18n.js           строки интерфейса RU/EN (без DOM — тестируется в node)
js/tiers.js          марки полос тиров и перевод старых сохранений (без DOM — тестируется в node)
js/html2canvas.min.js   экспорт в PNG (локально, офлайн)
assets/              картинки макета + шрифты
  poster/            всё, что вырезано из PSD «макнеми тир под сайт» (актуальный вид)
    bg.webp / bg-m.webp / bg-export.jpg   фон (сакура/храм/водопад), уже с цветокором
    petals.webp / petals-m.webp / petals-export.png   лепестки и декор
    band.webp / band.png                  плашка тира
    logo-bf.png                           логотип Blox Fruits
    marks.png                             марки шапки (молния + GLH + MK)
    badge-*.png / dot-*.png / trend-*.png значки легенды
    trend-new.png / trend-wip.png         они же на карточках предметов (NEW и «?»)
  bg.png / petals.png / bg-stage*.webp    фон прошлого макета (не подключён)
  dots-band.png      полутоновые точки плашки тира прошлого макета (не подключён)
  dots-tile.png      полутоновая текстура дорожки
  brand-logos.png    логотип шапки прошлого макета (не подключён)
  logo-bolt.png / logo-glh.png / logo-mk.png   логотипы тиров, те же марки, что в шапке
  logo-flame.png     марка тира прошлого макета (не подключён — старые сохранения переводятся на logo-bolt.png)
  bf-logo-trim.png   логотип Blox Fruits прошлого макета (не подключён)
  badge-*.png / dot-*.png / trend-*.png   значки на карточках предметов и в модалке
  icon-sample.png    стандартная иконка предмета
  fonts/Bootshaus/Bootshaus-Regular.ttf   шрифт легенды и подвала
  fonts/ProtoSans56.otf                   шрифт заголовка и даты
```

Файлы прошлого макета оставлены намеренно: деплой копирует дерево поверх, ничего
не удаляя, так что удаление из репозитория всё равно не убрало бы их с сервера —
зато сломало бы откат, если новый вид не зайдёт.

## Размер иконок

Иконки предметов хранятся с длинной стороной не больше **256 px**
(`ICON_MAX_SIDE` в `api/lib/images.php`). Порог не косметический: на экране
иконка занимает максимум ~130 device-px, а Safari держит в памяти
**распакованный** битмап целиком. Исходники по 800×800 давали 63 МБ битмапов на
полный тирлист — вкладка начинала выбрасывать декодированные картинки, и
иконки пропадали прямо во время прокрутки.

Сжатие применяется при загрузке автоматически. Картинки, загруженные раньше,
переводятся разово:

```
php tools/downscale-images.php --dry-run   # только отчёт
php tools/downscale-images.php             # сжать и перепривязать
```

На боевом хостинге `config.php` лежит над веб-рутом, вне клона репозитория,
поэтому туда нужно указать путь явно:

```
php tools/downscale-images.php --config=/home/maknemyt/config.php --dry-run
```

## Тесты

```
bash tests/run_all.sh
```

PHP-тесты покрывают API, node-тесты (`tests/*_test.mjs`) — словарь интерфейса,
язык описаний и марки полос тиров.
Если node не установлен, его часть пропускается с явным предупреждением.

## Что стоит добавить позже

- Реальные иконки предметов (сейчас одна стандартная — остальные загружаются вручную в редакторе).
