User-agent: *
Allow: /

# Служебные эндпоинты и загруженные картинки индексировать незачем.
Disallow: /api/
Disallow: /images/

# Админка — внутренние страницы (/admin — редактор тирлиста, /admin/promo —
# реклама). Разметку они отдают только по паролю, эта строка лишь убирает их
# из выдачи. Медиакит на сайте не лежит вовсе — он в docs/, вне веб-корня.
Disallow: /admin

Sitemap: https://maknemytierlist.site/sitemap.xml
