# Выделение предмета свечением — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** админ включает у предмета золотое свечение по контуру в окне редактирования, посетитель видит его на тирлисте.

**Architecture:** у предмета новое булево поле `glow`. Окно редактирования получает сегмент `#mGlow`, `renderCell` ставит карточке класс `glow`, а `styles.css` рисует свечение двумя `drop-shadow` на иконке (своими размерами на компьютере и на телефоне). Сервер и PNG-экспорт не меняются.

**Tech Stack:** обычные HTML/CSS/JS без сборки (`public_html/`), PHP 7.4 на бою, свой PHP-раннер тестов (`tests/lib.php`), `node --test`, Playwright для проверки в браузере (зависимостью проекта не является).

**Спека:** [2026-09-16-item-glow-design.md](../specs/2026-09-16-item-glow-design.md)

## Global Constraints

- Рабочее дерево — только worktree `C:/Users/nural/AppData/Local/Temp/claude/C--Users-nural-Desktop-Nexus-Tier-List/41188f68-515d-4365-80bf-11f007a4b460/scratchpad/wt-glow`, ветка `feat/item-glow` от `maknemy/master` (`c1e4f48`). Главное дерево `C:/Users/nural/Desktop/Nexus Tier List` принадлежит пользователю: там ничего не править и не переключать.
- В `public_html/**` пояснительных комментариев не писать — файлы отдаются посетителям как есть. В `tests/` и `tools/` комментарии допустимы.
- Файлы в рабочем дереве с CRLF. Правки вносить точной заменой с сохранением CRLF (помощник `patch.py` ниже). После каждой правки `git diff --stat` не должен показывать `Bin`.
- Поле предмета: `glow`, `true`/`false`; отсутствие поля = `false`.
- Сегмент редактора: `#mGlow`, кнопки `data-v=""` и `data-v="on"`; класс карточки `glow`; класс превью `glow` на `.icon-preview`.
- Свечение на компьютере: `filter: drop-shadow(0 0 .15cqw #ffe27f) drop-shadow(0 0 .45cqw rgba(255,190,40,.95));`
- Свечение на телефоне (≤640px): `filter: drop-shadow(0 0 .42cqw #ffe27f) drop-shadow(0 0 1.27cqw rgba(255,190,40,.95));`
- Превью в окне: `filter: drop-shadow(0 0 1.5px #ffe27f) drop-shadow(0 0 4.5px rgba(255,190,40,.95));`
- Ключи словаря: `modal.glow`, `modal.glowOn`, `modal.glowTitle` — в обеих таблицах, `ru` и `en`.
- PNG-экспорт, легенду, `og-tierlist.php`, калькулятор не трогать.
- Версии: `styles.css` 56 → 57, `app.js` 73 → 74, `i18n.js` 41 → 42 на `index.php`, `news.php`, `home.php`, `calculator.php`.
- Поставка: `git push -u maknemy feat/item-glow`, PR в `master` репозитория `kannurali/MaknemyTierlist`. Мержит пользователь.

## Подготовка (один раз, до задач)

Переменная для краткости во всех командах ниже:

```bash
WT=/c/Users/nural/AppData/Local/Temp/claude/C--Users-nural-Desktop-Nexus-Tier-List/41188f68-515d-4365-80bf-11f007a4b460/scratchpad/wt-glow
SP=/c/Users/nural/AppData/Local/Temp/claude/C--Users-nural-Desktop-Nexus-Tier-List/41188f68-515d-4365-80bf-11f007a4b460/scratchpad
```

- [ ] **Помощник правок `$SP/patch.py`** (вне репозитория). Писать инструментом Write, не heredoc: в правках есть обратные слэши.

```python
import sys


def apply(path, edits):
    raw = open(path, 'rb').read()
    if b'\x00' in raw:
        sys.exit(f'{path}: NUL byte before patching')
    crlf = b'\r\n' in raw
    text = raw.decode('utf-8')
    if crlf:
        text = text.replace('\r\n', '\n')
    for old, new in edits:
        n = text.count(old)
        if n != 1:
            sys.exit(f'{path}: anchor found {n} times: {old[:70]!r}')
        text = text.replace(old, new)
    if crlf:
        text = text.replace('\n', '\r\n')
    open(path, 'wb').write(text.encode('utf-8'))
    print('patched', path)
```

Каждая задача кладёт свои правки в `$SP/task<N>.py` вида `from patch import apply` + вызовы `apply(path, [(r'''старое''', r'''новое'''), ...])` и запускает `cd $SP && python task<N>.py`. Якорь должен встречаться в файле ровно один раз — иначе скрипт останавливается, ничего не записав в этот файл.

- [ ] **`config.php` в корне worktree** (в `.gitignore`, в коммит не попадает). `index.php` без него падает на `require`, а пустая база ему не мешает: превью ссылки откатывается на статичную картинку.

```php
<?php
return [
    'dsn'        => 'sqlite::memory:',
    'db_user'    => '',
    'db_pass'    => '',
    'admin_hash' => '',
    'images_dir' => __DIR__ . '/public_html/images',
];
```

- [ ] **Playwright рядом с проверками** (`tools/node_modules`, `tools/package.json` — в `.gitignore`). Версия та же, что в главном дереве, чтобы подошли уже скачанные браузеры:

```bash
cd $WT/tools && npm init -y >/dev/null && npm install playwright@1.62.1
```

- [ ] **PHP-сервер над `public_html` worktree** — запись в `C:/Users/nural/Desktop/Nexus Tier List/.claude/launch.json` (каталог `.claude/` в `.gitignore`), затем `preview_start` с именем `nexus-glow`:

```json
{
  "name": "nexus-glow",
  "runtimeExecutable": "C:/xampp/php/php.exe",
  "runtimeArgs": ["-S", "127.0.0.1:8880", "-t", "C:/Users/nural/AppData/Local/Temp/claude/C--Users-nural-Desktop-Nexus-Tier-List/41188f68-515d-4365-80bf-11f007a4b460/scratchpad/wt-glow/public_html"],
  "port": 8880
}
```

- [ ] **Обёртка PHP с GD для тестов** (`images_test.php` без GD даёт 18 ложных падений):

```bash
printf '#!/usr/bin/env bash\nexec /c/xampp/php/php.exe -d extension=gd "$@"\n' > $SP/php-gd.sh && chmod +x $SP/php-gd.sh
```

---

### Task 1: Проверка в браузере (сначала красная)

**Files:**
- Create: `tools/check-item-glow-ui.mjs`

**Interfaces:**
- Consumes: сервер `http://127.0.0.1:8880` из подготовки.
- Produces: `node tools/check-item-glow-ui.mjs [base]` — печатает `ok`/`FAIL` на каждую проверку и выходит с кодом 1 при любом `FAIL`. Задачи 2 и 3 делают её зелёной.

- [ ] **Step 1: Написать проверку** — файл `tools/check-item-glow-ui.mjs` целиком:

```js
// Browser check for the item glow ("Выделение" in the item editor).
//
//   node tools/check-item-glow-ui.mjs [http://127.0.0.1:8880]
//
// tests/tags_test.php pins the markup, the dictionary and the CSS as text.
// This drives the real page: the editor field and its live preview, the
// published payload, the colour under hover, the phone size, and that the
// PNG export still produces a file.
//
// The tier list comes from a fixture: api/tierlist.php, api/state.php,
// api/promo.php and api/save.php are answered here, so the check needs no
// database content and writes nothing. The admin role is granted the way
// admin.php grants it — window.NX_ADMIN_PAGE before app.js — plus a positive
// api/session.php, so no password is involved.
//
// Needs Playwright (install line in tools/make-mediakit-pdf.mjs) and a PHP
// server over public_html with a config.php next to it; any DSN will do,
// index.php survives an empty database:
//
//   php -S 127.0.0.1:8880 -t public_html
import { chromium } from 'playwright';
import { readFileSync } from 'node:fs';

const BASE = process.argv[2] || 'http://127.0.0.1:8880';
const GOLD = '255, 226, 127';
const CYAN = '79, 214, 255';

const item = (id, name, value, extra = {}) => ({
    id, name, value, icon: 'assets/icon-sample.png', type: 'f', demand: 'green', trend: '',
    desc: '', descEn: '', terms: '', termsEn: '', tag: '', tagEn: '', flag: false, wip: false,
    ...extra,
});

// «old» — предмет из сохранения, сделанного до появления поля glow.
const FIXTURE = {
    title: 'MAKNEMY\nTIER LIST',
    date: '16.09.2026',
    autoSort: false,
    filters: { configurators: true, fruits: true, perms: false, passes: true },
    ad: { text: '', image: '', link: '' },
    tiers: [{
        id: 't1', label: 'MK', logo: 'assets/logo-mk.png',
        items: [
            item('lit', 'Glowing', '3000', { glow: true }),
            item('off', 'Plain', '2000', { glow: false }),
            item('old', 'Legacy', '1000'),
        ],
    }],
    _rev: 1,
};

const browser = await chromium.launch();
let failed = 0;

function check(name, ok, detail) {
    console.log(`${ok ? 'ok  ' : 'FAIL'} - ${name}${ok ? '' : '   ' + JSON.stringify(detail)}`);
    if (!ok) failed++;
}

async function session(name, { admin = false, mobile = false }, body) {
    const context = await browser.newContext({
        viewport: mobile ? { width: 390, height: 844 } : { width: 1440, height: 1000 },
        isMobile: mobile,
        hasTouch: mobile,
        locale: 'ru-RU',
        acceptDownloads: true,
    });
    const page = await context.newPage();
    page.setDefaultTimeout(10000);
    // php -S serves one connection at a time, and a speculative connection
    // Chromium opens without sending a request can hold it for ~10 s.
    page.setDefaultNavigationTimeout(45000);
    const saved = [];
    const errors = [];
    page.on('pageerror', e => errors.push(String(e)));
    const json = data => ({ status: 200, contentType: 'application/json', body: JSON.stringify(data) });
    await page.route('**/api/state.php*', r => r.fulfill(json({ rev: 1, likes: 0 })));
    await page.route('**/api/tierlist.php*', r => r.fulfill(json({ tierlist: FIXTURE, likes: 0 })));
    await page.route('**/api/promo.php*', r => r.fulfill(json({ v: 1, rev: 0, campaigns: [] })));
    await page.route('**/api/session.php*', r => r.fulfill(json({ admin })));
    await page.route('**/api/save.php*', r => {
        saved.push(JSON.parse(r.request().postData() || 'null'));
        return r.fulfill(json({ ok: true, rev: 2 }));
    });
    if (admin) await page.addInitScript(() => { window.NX_ADMIN_PAGE = true; });
    try {
        await page.goto(BASE + '/index.php', { waitUntil: 'domcontentloaded' });
        await page.waitForSelector('.cell[data-id="old"]');
        await body(page, saved);
        check(`${name}: без ошибок на странице`, errors.length === 0, errors);
    } catch (e) {
        check(`${name}: прогон`, false, String(e).split('\n')[0]);
    } finally {
        await context.close();
    }
}

const iconFilter = (page, id) =>
    page.$eval(`.cell[data-id="${id}"] .cell-icon img`, el => getComputedStyle(el).filter);
const glowing = (page, id) =>
    page.$eval(`.cell[data-id="${id}"]`, el => el.classList.contains('glow'));
const cellWidth = (page, id) =>
    page.$eval(`.cell[data-id="${id}"]`, el => el.getBoundingClientRect().width);

// Размытие золотого ядра в px. Делённое на ширину ячейки, оно сравнимо
// между компьютером и телефоном.
const coreBlur = filter => {
    const m = new RegExp(`drop-shadow\\(rgba?\\(${GOLD}[^)]*\\) 0px 0px ([\\d.]+)px\\)`).exec(filter);
    return m ? Number(m[1]) : NaN;
};

let deskRatio = NaN;

await session('компьютер', {}, async page => {
    check('класс glow только у предмета со свечением',
        (await glowing(page, 'lit')) && !(await glowing(page, 'off')) && !(await glowing(page, 'old')), null);
    const lit = await iconFilter(page, 'lit');
    check('свечение золотое', lit.includes(GOLD), lit);
    deskRatio = coreBlur(lit) / (await cellWidth(page, 'lit'));
    const off = await iconFilter(page, 'off');
    check('у остальных золота нет', !off.includes(GOLD), off);

    await page.hover('.cell[data-id="lit"]');
    await page.waitForTimeout(300);
    const hovered = await iconFilter(page, 'lit');
    check('при наведении свечение остаётся золотым', hovered.includes(GOLD) && !hovered.includes(CYAN), hovered);

    await page.hover('.cell[data-id="off"]');
    await page.waitForTimeout(300);
    const plain = await iconFilter(page, 'off');
    check('обычный предмет при наведении голубой, как раньше', plain.includes(CYAN), plain);
});

await session('телефон', { mobile: true }, async page => {
    const lit = await iconFilter(page, 'lit');
    check('телефон: свечение золотое', lit.includes(GOLD), lit);
    const ratio = coreBlur(lit) / (await cellWidth(page, 'lit'));
    check('телефон: свечение заметнее, чем на компьютере', ratio > deskRatio * 1.3, { ratio, deskRatio });
    const off = await iconFilter(page, 'off');
    check('телефон: у остальных фильтра нет', off === 'none', off);
});

await session('админка', { admin: true }, async (page, saved) => {
    await page.waitForSelector('#tbToggles:not([hidden])');
    await page.evaluate(() => {
        const t = document.querySelector('#editToggle');
        t.checked = true;
        t.dispatchEvent(new Event('change'));
    });
    await page.waitForSelector('#stage.editing');

    const active = v => page.$eval(`#mGlow button[data-v="${v}"]`, b => b.classList.contains('active'));
    const preview = () => page.$eval('.icon-preview', el => el.classList.contains('glow'));
    const openItem = async id => {
        await page.click(`.cell[data-id="${id}"]`);
        await page.waitForSelector('#modal:not([hidden])');
    };

    await openItem('lit');
    check('окно: у светящегося выбрано «Свечение»', (await active('on')) && !(await active('')), null);
    check('окно: превью светится', await preview(), null);
    const pf = await page.$eval('#mIconPreview', el => getComputedStyle(el).filter);
    check('окно: превью золотое', pf.includes(GOLD), pf);
    await page.click('#modalClose');

    await openItem('off');
    check('окно: у обычного выбрано «—»', (await active('')) && !(await active('on')), null);
    check('окно: превью обычного не светится', !(await preview()), null);
    await page.click('#mGlow button[data-v="on"]');
    check('окно: щелчок сразу зажигает превью', await preview(), null);
    await page.click('#modalClose');
    check('крестик без «Готово» свечение не ставит', !(await glowing(page, 'off')), null);

    await openItem('off');
    check('окно: превью сбрасывается при новом открытии', !(await preview()), null);
    await page.click('#mGlow button[data-v="on"]');
    await page.click('#mSave');
    check('«Готово» зажигает предмет', await glowing(page, 'off'), null);

    await openItem('lit');
    await page.click('#mGlow button[data-v=""]');
    check('окно: «—» гасит превью', !(await preview()), null);
    await page.click('#mSave');
    check('«Готово» гасит предмет', !(await glowing(page, 'lit')), null);

    await page.click('#btnSave');
    await page.waitForFunction(() => document.querySelector('#btnSave').classList.contains('clean'));
    const last = saved[saved.length - 1];
    const glowById = last ? Object.fromEntries(last.tiers[0].items.map(i => [i.id, i.glow])) : null;
    check('публикация уносит поле glow',
        !!glowById && glowById.off === true && glowById.lit === false, glowById);

    const [download] = await Promise.all([
        page.waitForEvent('download', { timeout: 60000 }),
        page.click('#btnPng'),
    ]);
    const sig = readFileSync(await download.path()).subarray(0, 8).toString('hex');
    check('PNG по-прежнему скачивается', sig === '89504e470d0a1a0a', sig);
});

await browser.close();
console.log(failed ? `\n${failed} FAILED` : '\nALL OK');
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: Запустить и убедиться, что красная по нужной причине**

Run: `cd $WT && node tools/check-item-glow-ui.mjs`
Expected: `FAIL - класс glow только у предмета со свечением`, `FAIL - свечение золотое`, `FAIL - телефон: …`, `FAIL - админка: прогон` с `#mGlow` в тексте ошибки; `ok - компьютер: без ошибок на странице`; в конце `N FAILED`, код выхода 1. Если падает на `waitForSelector('.cell[data-id="old"]')` — не работает фикстура или сервер, чинить до перехода дальше.

- [ ] **Step 3: Коммит**

```bash
cd $WT && git add tools/check-item-glow-ui.mjs && git commit -m "test(tierlist): проверка свечения предмета в браузере

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Поле в редакторе, данные, класс на карточке

**Files:**
- Modify: `public_html/index.php` (модалка, после поля «Тренд»)
- Modify: `public_html/js/i18n.js` (после `modal.wipTitle` в `ru` и в `en`)
- Modify: `public_html/js/app.js` (`defaultState`, `renderCell`, `addItem`, `openModal`, флаги, обработчик сегментов, «Готово»)
- Test: `tests/tags_test.php`

**Interfaces:**
- Consumes: ничего из других задач.
- Produces: `item.glow` (boolean); сегмент `#mGlow` (`data-v` `""` / `"on"`); класс `glow` на `.cell` и на `.icon-preview`; функция `syncGlowPreview()` в `app.js`. Задача 3 рисует свечение по этим классам.

- [ ] **Step 1: Написать падающий тест** — в `tests/tags_test.php` перед блоком

```php
// --------------------------------------------------------------------------
//  Группировка фильтров
```

вставить:

```php
// Выделение — самостоятельный флаг предмета, как NEW и «?», но своим полем:
// к тренду оно не относится, и в легенде его нет. Поле, которое забыли
// прочитать при открытии окна или записать при сохранении, теряется молча:
// админ включает свечение, жмёт «Готово», а предмет не светится.
test('выделение: поле в модалке, чтение, сохранение, класс на карточке', function () use ($PUB) {
    $s    = tag_read($PUB . '/index.php');
    $from = strpos($s, '<div class="seg" id="mGlow">');
    assert_true($from !== false, 'нужен сегмент выделения');
    assert_true($from > strpos($s, '<div class="seg" id="mTrend">'), 'выделение стоит после тренда');
    $seg  = substr($s, $from, strpos($s, '</div>', $from) - $from);
    preg_match_all('/data-v="([^"]*)"/', $seg, $v);
    assert_eq(['', 'on'], $v[1], 'два состояния: выключено и включено');
    assert_true(strpos($seg, 'data-i18n="modal.glowOn"') !== false, 'кнопка подписана ключом словаря');
    assert_true(strpos($seg, 'data-i18n-title="modal.glowTitle"') !== false, 'у кнопки есть подсказка');
    assert_true(strpos($s, '<label data-i18n="modal.glow">') !== false, 'поле подписано');

    $i18n = tag_read($PUB . '/js/i18n.js');
    foreach (['modal.glow', 'modal.glowOn', 'modal.glowTitle'] as $key) {
        assert_eq(2, preg_match_all('/"' . preg_quote($key, '/') . '":/', $i18n), "$key нужен на обоих языках");
    }

    $js = tag_read($PUB . '/js/app.js');
    assert_true(strpos($js, 'cell.className = item.glow ? "cell glow" : "cell";') !== false,
        'карточка получает класс glow');
    assert_true(strpos($js, 'flag: false, wip: false, glow: false,') !== false,
        'предметы шаблона без свечения');
    assert_true(strpos($js, 'flag: true, wip: false, glow: false,') !== false,
        'новый предмет без свечения');
    assert_true(strpos($js, 'it.glow = getSeg("#mGlow") === "on";') !== false,
        'сохранение забирает выделение');
    assert_true(strpos($js, '["#mDemand", "#mTrend", "#mGlow"]') !== false,
        'щелчок по сегменту выделения переключает кнопки');
    assert_true(strpos($js, 'function syncGlowPreview() { $(".icon-preview").classList.toggle("glow", getSeg("#mGlow") === "on"); }') !== false,
        'превью в окне следует за сегментом');
    assert_true(strpos($js, '$("#mGlow").addEventListener("click", syncGlowPreview);') !== false,
        'щелчок сразу обновляет превью');

    $a    = strpos($js, 'function openModal(');
    $b    = strpos($js, 'function closeModal(', (int)$a);
    assert_true($a !== false && $b !== false, 'нужны openModal и closeModal');
    $open = substr($js, (int)$a, (int)$b - (int)$a);
    assert_true(strpos($open, 'setSeg("#mGlow", it.glow ? "on" : "");') !== false,
        'openModal ставит выделение по предмету');
    assert_true(strpos($open, 'syncGlowPreview();') !== false,
        'openModal сбрасывает превью под открытый предмет');
});

```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `cd $WT && $SP/php-gd.sh tests/tags_test.php`
Expected: `ASSERT FAIL нужен сегмент выделения` и остальные новые утверждения; старые тесты `ok`; итог `N failures`, код выхода 1.

- [ ] **Step 3: Правки** — `$SP/task2.py`:

```python
from patch import apply

WT = 'C:/Users/nural/AppData/Local/Temp/claude/C--Users-nural-Desktop-Nexus-Tier-List/41188f68-515d-4365-80bf-11f007a4b460/scratchpad/wt-glow/'

apply(WT + 'public_html/index.php', [(
r'''          </div>
        </div>
      </div>
      <div class="modal-foot">
''',
r'''          </div>
        </div>
        <div class="field">
          <label data-i18n="modal.glow">Выделение</label>
          <div class="seg" id="mGlow">
            <button data-v="" class="active">—</button>
            <button data-v="on" class="seg-glow" data-i18n="modal.glowOn" data-i18n-title="modal.glowTitle" title="Золотое свечение вокруг предмета на тирлисте. В PNG не попадает">✦ Свечение</button>
          </div>
        </div>
      </div>
      <div class="modal-foot">
''')])

apply(WT + 'public_html/js/i18n.js', [
(
r'''      "modal.wipTitle":         "Показать значок «?» на предмете (цена под вопросом). Работает вместе с NEW",
''',
r'''      "modal.wipTitle":         "Показать значок «?» на предмете (цена под вопросом). Работает вместе с NEW",
      "modal.glow":             "Выделение",
      "modal.glowOn":           "✦ Свечение",
      "modal.glowTitle":        "Золотое свечение вокруг предмета на тирлисте. В PNG не попадает",
'''),
(
r'''      "modal.wipTitle":         "Show the “?” badge on this item (value uncertain). Works together with NEW",
''',
r'''      "modal.wipTitle":         "Show the “?” badge on this item (value uncertain). Works together with NEW",
      "modal.glow":             "Highlight",
      "modal.glowOn":           "✦ Glow",
      "modal.glowTitle":        "Gold glow around the item on the tier list. Not included in the PNG",
'''),
])

apply(WT + 'public_html/js/app.js', [
(
r'''      flag: false, wip: false,
''',
r'''      flag: false, wip: false, glow: false,
'''),
(
r'''    cell.className = "cell";
''',
r'''    cell.className = item.glow ? "cell glow" : "cell";
'''),
(
r'''tag: "", tagEn: "", flag: true, wip: false,
''',
r'''tag: "", tagEn: "", flag: true, wip: false, glow: false,
'''),
(
r'''    setSeg("#mTrend", it.trend || "");
    modal.hidden = false;
''',
r'''    setSeg("#mTrend", it.trend || "");
    setSeg("#mGlow", it.glow ? "on" : "");
    syncGlowPreview();
    modal.hidden = false;
'''),
(
r'''  function getFlag(name) { return flagBtn(name).classList.contains("active"); }
''',
r'''  function getFlag(name) { return flagBtn(name).classList.contains("active"); }
  function syncGlowPreview() { $(".icon-preview").classList.toggle("glow", getSeg("#mGlow") === "on"); }
'''),
(
r'''  ["#mDemand", "#mTrend"].forEach(sel => {
''',
r'''  ["#mDemand", "#mTrend", "#mGlow"].forEach(sel => {
'''),
(
r'''      btn.classList.add("active");
    });
  });
''',
r'''      btn.classList.add("active");
    });
  });
  $("#mGlow").addEventListener("click", syncGlowPreview);
'''),
(
r'''    it.wip = getFlag("wip");
''',
r'''    it.wip = getFlag("wip");
    it.glow = getSeg("#mGlow") === "on";
'''),
])
```

Порядок обработчиков на `#mGlow` важен: общий обработчик сегментов вешается раньше и успевает переставить `active`, потом `syncGlowPreview` читает уже новое значение.

Run: `cd $SP && python task2.py`
Expected: три строки `patched …`, ни одного `anchor found`.

- [ ] **Step 4: Запустить тест — зелёный**

Run: `cd $WT && $SP/php-gd.sh tests/tags_test.php && node --test tests/i18n_test.mjs && git diff --stat`
Expected: `0 failures`; i18n `fail 0`; в `--stat` три файла `public_html/…` и `tests/tags_test.php`, без `Bin`.

- [ ] **Step 5: Проверить в браузере, что поле и класс уже работают**

Run: `cd $WT && node tools/check-item-glow-ui.mjs`
Expected: `ok` у «класс glow только у предмета со свечением», у всех проверок окна, «публикация уносит поле glow» и «PNG по-прежнему скачивается». Красными остаются только проверки цвета: `свечение золотое`, `при наведении свечение остаётся золотым`, `телефон: свечение золотое`, `телефон: свечение заметнее, чем на компьютере`, `окно: превью золотое`. Их закрывает задача 3.

- [ ] **Step 6: Коммит**

```bash
cd $WT && git add public_html/index.php public_html/js/i18n.js public_html/js/app.js tests/tags_test.php && git commit -m "feat(tierlist): поле «Выделение» в окне предмета

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Золотое свечение в CSS

**Files:**
- Modify: `public_html/css/styles.css` (после `.cell-icon img {…}`; у `.icon-preview img`; у `.seg .trend.tr-new`; в телефонном блоке после `.cell-icon img { filter: none; }`)
- Test: `tests/tags_test.php`

**Interfaces:**
- Consumes: классы `glow` на `.cell` и `.icon-preview`, `.seg-glow` на кнопке — из задачи 2.
- Produces: итоговый вид; проверка из задачи 1 становится полностью зелёной.

- [ ] **Step 1: Написать падающий тест** — в `tests/tags_test.php` сразу после теста из задачи 2:

```php
// Свечение — золотой контур по форме иконки. Правило стоит ПОСЛЕ голубого
// правила наведения: специфичность у них одна, и иначе при наведении голубое
// перебивало бы золотое. На телефоне свои размеры: пропорциональное
// десктопному свечение там почти не видно. Телефонное «filter: none» у
// иконок менее специфично и свечение не гасит.
test('свечение предмета: золотой контур, наведение не гасит, на телефоне сильнее', function () use ($PUB) {
    $css  = str_replace("\r\n", "\n", tag_read($PUB . '/css/styles.css'));
    $rule = '/\.cell\.glow \.cell-icon img \{\n\s*filter: drop-shadow\(0 0 ([\d.]+)cqw #ffe27f\) drop-shadow\(0 0 ([\d.]+)cqw rgba\(255,190,40,\.95\)\);\n\s*\}/';
    assert_eq(2, preg_match_all($rule, $css, $m, PREG_OFFSET_CAPTURE), 'одно правило для компьютера, одно для телефона');
    if (count($m[0]) !== 2) { return; }

    $hover = strpos($css, '.cell:hover .cell-icon img {');
    $phone = strpos($css, '@media (max-width: 640px)');
    $none  = strpos($css, '  .cell-icon img { filter: none; }');
    assert_true($m[0][0][1] > $hover, 'золотое правило стоит после голубого наведения');
    assert_true($m[0][0][1] < $phone, 'правило для компьютера — вне телефонных блоков');
    assert_true($m[0][1][1] > $none, 'правило для телефона — после телефонного filter: none');
    assert_eq(['.15', '.42'], [$m[1][0][0], $m[1][1][0]], 'ядро: компьютер, телефон');
    assert_eq(['.45', '1.27'], [$m[2][0][0], $m[2][1][0]], 'ореол: компьютер, телефон');

    assert_true(strpos($css, '.icon-preview.glow img { filter: drop-shadow(0 0 1.5px #ffe27f) drop-shadow(0 0 4.5px rgba(255,190,40,.95)); }') !== false,
        'превью в окне светится тем же золотом');
    assert_true(strpos($css, '.seg .seg-glow { color: #ffe27f;') !== false,
        'кнопка «Свечение» подписана золотым');
});

```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `cd $WT && $SP/php-gd.sh tests/tags_test.php`
Expected: `ASSERT FAIL одно правило для компьютера, одно для телефона` (expected 2, got 0), остальные `ok`, код выхода 1.

- [ ] **Step 3: Правки** — `$SP/task3.py`:

```python
from patch import apply

WT = 'C:/Users/nural/AppData/Local/Temp/claude/C--Users-nural-Desktop-Nexus-Tier-List/41188f68-515d-4365-80bf-11f007a4b460/scratchpad/wt-glow/'

apply(WT + 'public_html/css/styles.css', [
(
r'''.cell-icon img {
  max-width: 92%; max-height: 100%;
  filter: drop-shadow(0 2px 4px rgba(0,0,0,.55));
  image-rendering: auto;
  pointer-events: none;
}
''',
r'''.cell-icon img {
  max-width: 92%; max-height: 100%;
  filter: drop-shadow(0 2px 4px rgba(0,0,0,.55));
  image-rendering: auto;
  pointer-events: none;
}
.cell.glow .cell-icon img {
  filter: drop-shadow(0 0 .15cqw #ffe27f) drop-shadow(0 0 .45cqw rgba(255,190,40,.95));
}
'''),
(
r'''.icon-preview img { max-width: 90%; max-height: 80%; }
''',
r'''.icon-preview img { max-width: 90%; max-height: 80%; }
.icon-preview.glow img { filter: drop-shadow(0 0 1.5px #ffe27f) drop-shadow(0 0 4.5px rgba(255,190,40,.95)); }
'''),
(
r'''.seg .trend.tr-new { width: 34px; }
''',
r'''.seg .trend.tr-new { width: 34px; }
.seg .seg-glow { color: #ffe27f; text-shadow: 0 0 6px rgba(255,190,40,.8); }
'''),
(
r'''  .cell-icon img { filter: none; }
''',
r'''  .cell-icon img { filter: none; }
  .cell.glow .cell-icon img {
    filter: drop-shadow(0 0 .42cqw #ffe27f) drop-shadow(0 0 1.27cqw rgba(255,190,40,.95));
  }
'''),
])
```

Run: `cd $SP && python task3.py`
Expected: `patched …styles.css`.

- [ ] **Step 4: Тест — зелёный**

Run: `cd $WT && $SP/php-gd.sh tests/tags_test.php && git diff --stat`
Expected: `0 failures`; в `--stat` `public_html/css/styles.css` и `tests/tags_test.php`, без `Bin`.

- [ ] **Step 5: Проверка в браузере — полностью зелёная**

Run: `cd $WT && node tools/check-item-glow-ui.mjs`
Expected: все строки `ok`, в конце `ALL OK`, код выхода 0.

- [ ] **Step 6: Посмотреть глазами** — снять тирлист с живыми данными (скрипт в `$SP`, не в репозитории): тот же сервер, но `api/tierlist.php`, `api/state.php` и `/images/*` проксируются на `https://maknemy.com`, `api/promo.php` — пустой документ; трём предметам первого ряда ставится `glow: true` в ответе `tierlist.php`. Снимки: компьютер 1440, телефон 390 (`isMobile`), компьютер с наведением на светящийся предмет, открытое окно редактирования со включённым свечением. Проверить: золото видно на тёмных и на ярких иконках, на телефоне заметно, соседние ячейки свечение не задевает, превью в окне не обрезано до уродства.

- [ ] **Step 7: Коммит**

```bash
cd $WT && git add public_html/css/styles.css tests/tags_test.php && git commit -m "feat(tierlist): золотое свечение выделенного предмета

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: Кэш, полный прогон, PR

**Files:**
- Modify: `public_html/index.php`, `public_html/news.php`, `public_html/home.php`, `public_html/calculator.php` (версии)

**Interfaces:**
- Consumes: всё из задач 1–3.
- Produces: PR в `kannurali/MaknemyTierlist`.

- [ ] **Step 1: Поднять версии** — `$SP/task4.py`:

```python
from patch import apply

PUB = 'C:/Users/nural/AppData/Local/Temp/claude/C--Users-nural-Desktop-Nexus-Tier-List/41188f68-515d-4365-80bf-11f007a4b460/scratchpad/wt-glow/public_html/'

apply(PUB + 'index.php', [
    ('css/styles.css?v=56"', 'css/styles.css?v=57"'),
    ('js/i18n.js?v=41"', 'js/i18n.js?v=42"'),
    ('js/app.js?v=73"', 'js/app.js?v=74"'),
])
for page in ('news.php', 'home.php', 'calculator.php'):
    apply(PUB + page, [('js/i18n.js?v=41"', 'js/i18n.js?v=42"')])
```

Run: `cd $SP && python task4.py && cd $WT && git grep -n "i18n.js?v=" -- public_html`
Expected: четыре `patched`; все четыре страницы с `i18n.js?v=42`.

- [ ] **Step 2: Полный прогон**

```bash
cd $WT && PHP=$SP/php-gd.sh bash tests/run_all.sh 2>&1 | tail -5
cd $WT && export PHP=C:/xampp/php/php.exe && for t in tests/*_test.mjs; do node --test "$t" 2>&1 | grep -E "^# (pass|fail)"; done
cd $WT && node tools/check-item-glow-ui.mjs | tail -3
```

Expected: PHP-часть без `ASSERT FAIL`/`ERROR` (раннер доходит до `== tests/…_test.mjs ==` или падает только на `news_test.mjs` из-за обёртки — это известно, node-часть гоняется второй командой); в node-части каждый файл `# fail 0`; браузерная проверка `ALL OK`.

- [ ] **Step 3: Чистота дерева**

Run: `cd $WT && git status --short && git diff --stat maknemy/master -- . && python -c "import subprocess;fs=subprocess.check_output(['git','ls-files','-z']).split(b'\0');bad=[f for f in fs if f and f.endswith((b'.js',b'.mjs',b'.php',b'.css',b'.md')) and b'\0' in open(f,'rb').read()];print('NUL:',bad)"`
Expected: в статусе только четыре страницы с версиями (`config.php`, `tools/node_modules`, `tools/package*.json` в `.gitignore` и не видны); `NUL: []`; нет `Bin` в `--stat`.

- [ ] **Step 4: Коммит**

```bash
cd $WT && git add public_html/index.php public_html/news.php public_html/home.php public_html/calculator.php && git commit -m "chore(cache): версии styles.css, app.js и i18n.js под свечение

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

- [ ] **Step 5: Push и PR**

```bash
cd $WT && git fetch maknemy && git log --oneline maknemy/master -1
```

Если `maknemy/master` ушёл вперёд от `c1e4f48` — `git merge maknemy/master`, заново шаг 2 и сверить версии `?v=` (их могли поднять в другом PR — тогда нашим нужно быть выше).

```bash
cd $WT && git push -u maknemy feat/item-glow
gh pr create --repo kannurali/MaknemyTierlist --base master --head feat/item-glow --title "Выделение предмета золотым свечением" --body-file $SP/pr-body.md
```

`$SP/pr-body.md` — что сделано, как проверено (тесты, браузерная проверка, скриншоты), что свечение не попадает в PNG и рассчитано на небольшое число предметов; в конце строка `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
